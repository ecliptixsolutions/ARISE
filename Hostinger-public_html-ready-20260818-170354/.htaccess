Options -MultiViews
RewriteEngine On
RewriteBase /

# Serve existing files and folders directly.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# React/TanStack client-side routes fallback.
RewriteRule ^ index.html [L]
